# Cipherly Frontend

## Setup

1. Change to the `frontend` folder.

   ```sh
   cd frontend
   ```

1. Install NPM dependencies.

   ```sh
   npm install
   ```

## Run the Dev Server

```sh
npm run dev
```
